pluses in the front they the same, if they in the back it be a trap
LAMBDA suck, they are just regular loops and function but just complex
stars have points AND theres references
referances are aliases
pointers are there own object
; at the end of a class
vector<t> uses .size()
array[] uses sizeof()
